name: flutter_laboratory
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.0
  provider: ^4.3.2+3 #状态管理 google推荐
  dio: ^3.0.10 #网络请求库
  json_annotation: ^3.0.1 #json解析
  flutter_screenutil: ^4.0.2+2 #屏幕适配f
  event_bus: ^1.1.1 #一个使用Dart流进行解耦应用程序的简单事件总线的库。
  shared_preferences: ^0.5.10 # 用于读写简单键值对的Flutter插件
  flutter_swiper: ^1.1.6  #轮播
  fluttertoast: ^7.1.6 #toast提示
  bot_toast: ^3.0.5
  dio_log : ^1.3.5


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - static/images/

